import { DiariosClient } from "./DiariosClient";

export const metadata = {
  title: "Control - Diarios",
  description: "Funciones obligatorias por operador en Control Máster.",
};

export default function DiariosPage() {
  return <DiariosClient />;
}
